extends layout

block content
  br
  h1 Login
  br
  br
  form(method='POST', action='/users/login')
    #form-group
      label Username:
      input.form-control(name='username', type="text")
    #form-group
      label Password:
      textarea.form-control(name='password', type="password")
    br
    input.btn.btn-primary(type="submit", value='Submit')